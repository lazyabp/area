version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=AreaKit_Identity
      - AreaKit_DB=AreaKit_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
